# Plant Binary Model Output

Training outputs are written here by:

```powershell
python tools/ml/train_binary_model.py --dataset datasets/plant_binary --output models/plant_binary
```

Expected outputs:

```text
labels.txt
metadata.json
plant_binary_float.tflite
plant_binary_int8.tflite
plant_binary_model.keras
```

